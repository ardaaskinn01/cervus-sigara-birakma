import 'package:flutter/material.dart';

class AppColors {
  // Primary (Ana Renk)
  static const Color primary = Color(0xFF22C55E); // Sağlıklı yeşil

  // Background
  static const Color background = Color(0xFFF8FAFC); // Kırık beyaz
  static const Color card = Color(0xFFFFFFFF); // Kartlar

  // Secondary
  static const Color secondary = Color(0x0EA5E9); // Nefes / Ferahlık (Mavi) - NOT: Hata olabilir, 0x0EA5E9 6 haneli değil. 0xFF0EA5E9 olmalı.
  static const Color breathBlue = Color(0xFF0EA5E9);

  // Accent (Kritik an / Craving)
  static const Color accent = Color(0xFFF97316); // Turuncu (Sigara isteği butonu)

  // Soft UI Tonları
  static const Color border = Color(0xFFE2E8F0);
  static const Color secondaryText = Color(0xFF94A3B8);
  
  // Extra Utils
  static const Color primaryDark = Color(0xFF16A34A);
}
